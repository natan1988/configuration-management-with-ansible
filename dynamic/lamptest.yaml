---
- name: install LAMP stack
  hosts: all
  become: yes
  become_method: sudo 
  tasks: 
    - name: install multiple packages
      package: 
        name: "{{item}}"
        state: latest
      with_items: 
        - httpd
        - mysql
        - mysql-server
        - php
        - php-mysql
    - name: start apache
      systemd:
        name: httpd
        state: started
        enabled: yes
    - name: start mysql
      systemd:  
        name: mysql
        state: started
        enabled: yes

  


    
